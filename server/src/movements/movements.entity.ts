export enum MovStatus {
  pending = 'Pending',
  successful = 'Successful',
  rejected = 'Rejected',
}

export enum MovType {
  payment = 'Payment',
  transfers = 'Transfers',
  withdraw = 'Withdraw',
}
